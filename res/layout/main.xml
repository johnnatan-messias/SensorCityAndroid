<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:gravity="center"
    android:orientation="vertical" >

    <include layout="@layout/include_header" >
    </include>

    <util.DashboardLayout
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="1" >

        <Button
            style="@style/texto12Negrito"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@null"
            android:drawablePadding="10dp"
            android:drawableTop="@drawable/main_sensors"
            android:onClick="onClickSensorsList"
            android:text="@string/button_sensors" >
        </Button>

        <Button
            style="@style/texto12Negrito"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@null"
            android:drawablePadding="10dp"
            android:drawableTop="@drawable/main_map"
            android:onClick="onClickMapSensors"
            android:text="@string/button_map" >
        </Button>
    </util.DashboardLayout>

    <include layout="@layout/include_footer" >
    </include>

</LinearLayout>